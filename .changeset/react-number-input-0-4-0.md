---
"@mshafiqyajid/react-number-input": minor
---

Add hold-to-repeat acceleration (`repeat` prop), scrubable label drag (`scrubable` / `scrubPixels`), `largeStep` alias, and digit scroll animation on stepper interaction.
