# Changesets

This folder is managed by [Changesets](https://github.com/changesets/changesets).

To record a change for the next release, run:

```bash
npm run changeset
```

Each `*.md` file in this folder describes a pending change. They get consumed and deleted when versions are bumped.
